/**
 * @file index.ts
 * @description Anchor Metadata Schema Validation for VDR documents
 * Enforces consistent and typed schemas for document metadata.
 */
export interface MetadataSchema {
    fields: Record<string, {
        type: 'string' | 'number' | 'date' | 'enum';
        required?: boolean;
        enum?: string[];
        maxLength?: number;
        pattern?: RegExp;
        description?: string;
    }>;
}
export interface ValidationSuccess {
    valid: true;
    errors: [];
}
export interface ValidationFailure {
    valid: false;
    errors: string[];
}
export type ValidationResult = ValidationSuccess | ValidationFailure;
/**
 * Validates metadata against a provided schema.
 */
export declare function validateMetadata(metadata: Record<string, string | number | boolean | any>, schema: MetadataSchema): ValidationResult;
export declare const LEGAL_CONTRACT_SCHEMA: MetadataSchema;
export declare const CLINICAL_TRIAL_SCHEMA: MetadataSchema;
//# sourceMappingURL=index.d.ts.map