import { HashAlgorithm, HashOptions } from '../types';
/**
 * Returns metadata about a supported hashing algorithm.
 */
export declare function getAlgorithmInfo(algorithm: HashAlgorithm): {
    bits: number;
    outputLength: number;
    recommended: boolean;
    label: string;
} | {
    bits: number;
    outputLength: number;
    recommended: boolean;
    label: string;
} | {
    bits: number;
    outputLength: number;
    recommended: boolean;
    fast: boolean;
    label: string;
} | {
    bits: number;
    outputLength: number;
    recommended: boolean;
    label: string;
    warning: string;
};
/**
 * Compute the hash of a document buffer using the specified algorithm.
 *
 * @param file - Document as Buffer
 * @param options - Hashing configuration (algorithm, encoding)
 * @returns Lowercase hex or base64 digest
 */
export declare function hashDocument(file: Buffer, options?: HashOptions): Promise<string>;
/**
 * Compute hash of a file on disk (Node.js only).
 */
export declare function hashFile(filePath: string, options?: HashOptions): Promise<string>;
/**
 * Hash a file using streaming for large files.
 */
export declare function hashFileStream(filePath: string, options?: HashOptions): Promise<string>;
/**
 * Hash with progress reporting for large files.
 */
export declare function hashFileWithProgress(filePath: string, onProgress?: (bytesProcessed: number, totalBytes: number) => void, options?: HashOptions): Promise<string>;
/**
 * Compute hash of a base64 string.
 */
export declare function hashBase64(base64: string, options?: HashOptions): Promise<string>;
/**
 * Compute hash from a Readable stream.
 */
export declare function hashStream(stream: any, options?: HashOptions): Promise<string>;
/**
 * Validate that a string is a valid hex digest for the given algorithm.
 */
export declare function isValidHash(hash: string, algorithm?: HashAlgorithm): boolean;
/**
 * Normalize a hash string.
 */
export declare function normalizeHash(hash: string): string;
//# sourceMappingURL=algorithms.d.ts.map