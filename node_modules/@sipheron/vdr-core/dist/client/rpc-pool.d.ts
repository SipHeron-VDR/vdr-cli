/**
 * RPC node pool with automatic failover.
 * Rotates through available nodes when one fails.
 */
export declare class RPCPool {
    private nodes;
    private currentIndex;
    private failures;
    constructor(nodes: string[]);
    getCurrent(): string;
    markFailed(node: string): void;
    markSuccess(node: string): void;
    private rotate;
}
export declare const DEFAULT_RPC_NODES: {
    devnet: string[];
    mainnet: string[];
};
//# sourceMappingURL=rpc-pool.d.ts.map